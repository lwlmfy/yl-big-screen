# yl-big-screen 组件文档

大屏可视化通用 Vue 3 组件库，提供大屏项目的标准布局、自适应缩放、面板容器和弹窗系统。

## 安装

```bash
# pnpm
pnpm install yl-big-screen

# 或 npm
npm install yl-big-screen
```

无需手动引入样式，组件会自动注入。

## 在线预览

👉 [在线 Demo 演示](https://lwlmfy.github.io/yl-big-screen)

---

## 组件列表

- [ProjectScreenRoot](#projectscreenroot) — 大屏根布局组件
- [ProjectScreenScale](#projectscreenscale) — 自适应缩放容器
- [ProjectPanelShell](#projectpanelshell) — 面板容器组件
- [CardSubTitle](#cardsubtitle) — 轻量卡片副标题组件
- [GlobalDialog](#globaldialog) — 独立弹窗组件
- [DialogContainer](#dialogcontainer) — 弹窗管理器
- [弹窗服务 API](#弹窗服务-api) — 编程式弹窗

---

## ProjectScreenRoot

大屏根布局组件，集成自适应缩放、顶部标题栏、内容区、弹窗容器，一个大屏项目只需要一个。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | `String` | `"大屏"` | 大屏标题 |
| width | `Number` | `1920` | 设计稿宽度 |
| height | `Number` | `1080` | 设计稿高度 |
| scaleMode | `String` | `"fixed-height"` | 缩放模式 |
| renderMode | `String` | `"transform"` | 渲染模式：`auto` / `transform` / `zoom` |
| alignX | `String` | `"center"` | 水平对齐：`start` / `center` / `end` |
| alignY | `String` | `"center"` | 垂直对齐：`start` / `center` / `end` |
| disabledScale | `Boolean` | `false` | 禁用缩放 |
| minScale | `Number` | `0` | 最小缩放比例 |
| maxScale | `Number` | `Infinity` | 最大缩放比例 |
| showHeader | `Boolean` | `true` | 是否显示顶部标题栏 |
| showDateTime | `Boolean` | `true` | 是否显示当前时间 |
| headerHeight | `Number` | `80` | 标题栏高度（px） |
| background | `String` | `"#07111f"` | 背景色 |
| pageBackgroundImage | `String` | `""` | 页面背景图 URL |
| headerBackgroundImage | `String` | `""` | 标题栏背景图 URL |
| contentPadding | `String` | `"0 10px 10px"` | 内容区内边距 |
| gap | `Number` | `18` | 标题栏与内容区间距 |
| enableMenuBridge | `Boolean` | `true` | 标题点击通过 postMessage 通知父窗口 |
| titleClickable | `Boolean` | `true` | 标题是否可点击 |
| overflowHidden | `Boolean` | `true` | 是否隐藏溢出 |
| useResizeObserver | `Boolean` | `true` | 使用 ResizeObserver 监听 |
| listenWindowResize | `Boolean` | `true` | 监听 window resize 事件 |
| containerSelector | `String` | `""` | 自定义缩放参考容器的 CSS 选择器 |
| teleportDialog | `Boolean` | `true` | 弹窗是否 Teleport 到 body |

### Slots

| 插槽 | 作用域数据 | 说明 |
|---|---|---|
| `default` | `ScreenState` | 主内容区 |
| `title` | `ScreenState` | 自定义标题内容 |
| `header-left` | `ScreenState` | 标题栏左侧区域 |
| `header-right` | `ScreenState` | 标题栏右侧区域（不传则显示时间） |

### ScreenState

```ts
{
  containerWidth: number
  containerHeight: number
  contentWidth: number
  contentHeight: number
  scale: number
  scaleX: number
  scaleY: number
  offsetX: number
  offsetY: number
}
```

### Events

| 事件 | 参数 | 说明 |
|---|---|---|
| `title-click` | `(event: Event)` | 点击标题时触发 |
| `scale-update` | `(state: ScaleState)` | 缩放状态更新 |

### 代码示例

```vue
<template>
  <ProjectScreenRoot
    title="智慧城市大屏"
    :width="1920"
    :height="1080"
    scale-mode="fixed-height"
    page-background-image="/bg/main-bg.svg"
    header-background-image="/bg/header-bg.png"
  >
    <template #header-left>
      <span>2026-05-28 12:00:00</span>
    </template>
    <template #header-right>
      <button @click="openMenu">菜单</button>
    </template>

    <div style="display: grid; grid-template-columns: 400px 1fr 400px; gap: 16px; height: 100%;">
      <ProjectPanelShell title="左侧面板">
        <p>内容...</p>
      </ProjectPanelShell>
      <ProjectPanelShell title="中间面板">
        <p>内容...</p>
      </ProjectPanelShell>
      <ProjectPanelShell title="右侧面板">
        <p>内容...</p>
      </ProjectPanelShell>
    </div>
  </ProjectScreenRoot>
</template>

<script setup>
import { ProjectScreenRoot, ProjectPanelShell } from 'yl-big-screen'
</script>
```

---

## ProjectScreenScale

自适应缩放容器组件，ProjectScreenRoot 底层就是用的它，也可以独立使用。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| width | `Number` | `1920` | 设计稿宽度 |
| height | `Number` | `1080` | 设计稿高度 |
| mode | `String` | `"fixed-height"` | 缩放模式：`contain` / `cover` / `stretch` / `width` / `height` / `fixed-height` / `fixed-width` |
| renderMode | `String` | `"transform"` | 渲染模式 |
| alignX | `String` | `"center"` | 水平对齐 |
| alignY | `String` | `"center"` | 垂直对齐 |
| disabled | `Boolean` | `false` | 禁用缩放 |
| minScale | `Number` | `0` | 最小缩放 |
| maxScale | `Number` | `Infinity` | 最大缩放 |
| overflowHidden | `Boolean` | `true` | 隐藏溢出 |
| useResizeObserver | `Boolean` | `true` | 使用 ResizeObserver |
| transitionDuration | `Number` | `180` | 过渡动画时长（ms） |

### Slots

| 插槽 | 作用域数据 | 说明 |
|---|---|---|
| `default` | `ScreenState` | 缩放内容区 |

### 代码示例

```vue
<template>
  <ProjectScreenScale :width="1920" :height="1080" mode="fixed-height">
    <div class="my-content">
      <!-- 1920x1080 的设计稿内容 -->
    </div>
  </ProjectScreenScale>
</template>

<script setup>
import { ProjectScreenScale } from 'yl-big-screen'
</script>
```

---

## ProjectPanelShell

面板容器组件，浮动标题条 + 装饰边框 + 磨砂背景。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | `String` | `"标题"` | 面板标题 |
| showTitle | `Boolean` | `true` | 是否显示浮动标题条 |
| bodyClass | `String` | `""` | 内容区额外 CSS class |
| titleBackgroundImage | `String` | `""` | 自定义标题条背景图 |
| titleClickable | `Boolean` | `false` | 标题是否可点击 |

### Slots

| 插槽 | 说明 |
|---|---|
| `default` | 面板主体内容 |
| `title-icon` | 标题文字后面的图标区域，默认无内容 |

### Events

| 事件 | 说明 |
|---|---|
| `titleClick` | 标题可点击时，点击触发 |

### 代码示例

```vue
<template>
  <!-- 默认带标题 -->
  <ProjectPanelShell title="监控指标">
    <div>内容...</div>
  </ProjectPanelShell>

  <!-- 标题带图标 -->
  <ProjectPanelShell title="监控指标">
    <template #title-icon>
      <img src="/icons/info.svg" width="20" height="20" />
    </template>
    <div>内容...</div>
  </ProjectPanelShell>

  <!-- 无标题面板 -->
  <ProjectPanelShell :show-title="false">
    <div>内容...</div>
  </ProjectPanelShell>
</template>

<script setup>
import { ProjectPanelShell } from 'yl-big-screen'
</script>
```

---

## CardSubTitle

轻量卡片容器组件，无装饰边框，标题栏使用装饰背景图。适合嵌套在 ProjectPanelShell 内部做模块分区。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | `String` | `"标题"` | 模块标题 |
| showTitle | `Boolean` | `true` | 是否显示标题栏 |
| padding | `String` | `"0 14px"` | 内容区内边距，CSS padding 值 |

### Slots

| 插槽 | 说明 |
|---|---|
| `default` | 卡片主体内容 |
| `header-right` | 标题栏右侧区域 |

### 代码示例

```vue
<template>
  <CardSubTitle title="监控指标">
    <template #header-right>
      <button>更多</button>
    </template>
    <div>内容...</div>
  </CardSubTitle>

  <!-- 自定义内边距 -->
  <CardSubTitle title="监控指标" padding="12px 20px">
    <div>内容...</div>
  </CardSubTitle>
</template>

<script setup>
import { CardSubTitle } from 'yl-big-screen'
</script>
```

---

## GlobalDialog

独立弹窗组件，内部使用，一般不直接引用。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| title | `String` | — | 弹窗标题 |
| modelValue | `Boolean` | — | 控制显示/隐藏 |
| size | `String` | `"medium"` | 预设尺寸：`medium` / `large` / `full` |
| width | `String` | — | 自定义宽度 |
| height | `String` | — | 自定义高度 |
| closeOnClickMask | `Boolean` | `true` | 点击遮罩关闭 |

---

## DialogContainer

弹窗管理器，已在 ProjectScreenRoot 中内置，通常不需要手动引入。

---

## 弹窗服务 API

通过 `openBigScreenDialog` 以编程方式打开弹窗。

### 导入

```ts
import { openBigScreenDialog, closeDialog, dialogs } from 'yl-big-screen'
import type { DialogItem } from 'yl-big-screen'
```

### openBigScreenDialog(options)

打开弹窗，返回 Promise（弹窗关闭时 resolve）。

```ts
openBigScreenDialog({
  title: "数据详情",
  component: MyDialogContent,  // Vue 组件
  size: "medium",              // medium / large / full
  props: {                     // 传给组件的 props
    dataId: "123"
  },
  closeOnClickMask: true,
  onClose: (result) => {
    console.log("弹窗关闭:", result)
  },
  onOpen: () => {
    console.log("弹窗已打开")
  },
  onTitleClick: () => {
    console.log("点击了标题")
  }
}).then(result => {
  console.log("结果:", result)
})
```

弹窗内容组件会收到 `close` prop，用于关闭弹窗并传回数据：

```ts
// MyDialogContent.vue
const props = defineProps<{
  dataId: string
  close: (result?: any) => void
}>()

function handleConfirm() {
  props.close?.({ status: 'ok' })
}
```

### closeDialog(id, result?)

手动关闭指定弹窗。

### dialogs

响应式弹窗列表（`Ref<DialogItem[]>`），用于自定义弹窗逻辑。

---

## 本地开发

```bash
# pnpm
pnpm install
pnpm dev

# 或 npm
npm install
npm run dev
```

启动后打开 http://localhost:3100 查看组件效果。
